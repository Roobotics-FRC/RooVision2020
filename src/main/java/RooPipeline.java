import org.opencv.core.Mat;

public class RooPipeline extends GripPipeline {
    @Override
    public void process(Mat source0) {
        // Insert Sam's OpenCV mayhem here...
        super.process(source0);
    }
}
